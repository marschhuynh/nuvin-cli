---
"@nuvin/nuvin-cli": minor
---

Add history pagination and search to session browser. Sessions now load in pages of 20, with automatic load-more when scrolling near bottom. Search queries all sessions asynchronously with debounced input.
