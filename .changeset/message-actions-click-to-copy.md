---
"@nuvin/nuvin-cli": minor
---

Add click-to-copy with contextual message actions in alt mode. Left-click any message to show action bar with Copy (all messages), Edit (user messages), and Retry (assistant/tool messages). Add `copyTextToClipboard` utility for cross-platform text copying. Add `extractMessageContent` utility to extract plain text from any message type including tool calls. Add `MessageActionBar` component for inline action display.
