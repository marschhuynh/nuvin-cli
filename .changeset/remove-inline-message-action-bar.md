---
"@nuvin/nuvin-cli": patch
---

Remove `MessageActionBar` component and inline message actions. Consolidate all message actions (copy, edit, retry) into `MessageActionModal` for consistent interaction pattern.
