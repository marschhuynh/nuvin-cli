---
'@nuvin/nuvin-cli': minor
---

Add SendPipeline to orchestrate message sending with pre/post hooks. Extract lazy session init, LLM refresh, memory injection, config application, and context window checks into pipeline stages.
